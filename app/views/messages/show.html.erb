<div class="container mx-auto mt-8 px-4">
  <div class="max-w-2xl mx-auto bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
    <h1 class="text-2xl font-bold mb-4"><%= @message.title %></h1>
    <p class="text-gray-700 text-base mb-4"><%= @message.content %></p>
    <p class="text-sm text-gray-600">作成日時: <%= @message.created_at.strftime("%Y年%m月%d日 %H:%M") %></p>
    <%= link_to 'ホームに戻る', home_path, class: "mt-4 inline-block bg-teal-500 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
</div>